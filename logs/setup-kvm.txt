# 📂 Logs completos - Configuração KVM + Ubuntu Server 24.04.4 LTS

## 🖥️ Ambiente

* Host: Pop!_OS Linux
* Virtualização: KVM / QEMU / libvirt
* VM: Ubuntu Server 24.04.4 LTS
* Rede: NAT (virbr0 - 192.168.122.0/24)

---

# 🔧 COMANDOS EXECUTADOS NO HOST (Pop!_OS)

## Verificação de interfaces de rede

ip a

Resultado relevante:

* eno1 → 192.168.4.3
* virbr0 → 192.168.122.1 (rede da VM)

---

## Teste de conectividade com a VM

ping 192.168.122.119

Resultado:

* Comunicação OK (0% packet loss)

---

## Tentativa de acesso SSH (ERRO)

ssh aluisio@192.168.122.119/24

Erro:
Could not resolve hostname

---

## Correção do comando SSH

ssh aluisio@192.168.122.119

---

## Conexão estabelecida com sucesso

Welcome to Ubuntu 24.04.4 LTS

---

# 🖥️ COMANDOS EXECUTADOS NA VM (Ubuntu Server)

## Atualização do sistema

sudo apt update && sudo apt upgrade -y

---

## Verificação de rede

ip a

ping -c 6 google.com

---

## Verificação do serviço SSH

sudo systemctl status ssh

---

## Ativação do SSH

sudo systemctl enable --now ssh

---

## Verificação da porta SSH

sudo ss -tulpn | grep :22

---

## Verificação da rede KVM

ip a | grep virbr0

ping -c 7 192.168.122.1

---

## Tentativa de listar leases DHCP (libvirt)

sudo virsh net-dhcp-leases default

sudo virsh -c qemu:///system net-dhcp-leases default

---

## Forçar renovação de IP

sudo dhclient

ip a

---

## Revalidação do SSH

sudo systemctl enable --now ssh

sudo systemctl status ssh

sudo ss -tulpn | grep :22

---

## Histórico de comandos

history

---

# 🧠 PROBLEMAS ENCONTRADOS E SOLUÇÕES

## ❌ Erro de conexão SSH

Comando incorreto:
ssh aluisio@192.168.122.119/24

Erro:
Could not resolve hostname

### ✅ Solução

Uso correto:
ssh aluisio@192.168.122.119

---

## ❌ SSH não acessível inicialmente

### ✅ Solução

sudo systemctl enable --now ssh

Validação:
sudo ss -tulpn | grep :22

---

# 🚀 RESULTADO FINAL

* VM criada com sucesso no KVM
* Rede NAT funcional (virbr0)
* Comunicação host ↔ VM validada
* SSH funcionando corretamente
* Ambiente pronto para uso remoto

